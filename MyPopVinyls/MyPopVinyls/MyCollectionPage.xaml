<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyPopVinyls.MyCollectionPage">
    
    <ListView x:Name="popListView">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ViewCell.View>
                        <StackLayout>
                          <Label Text="Pop Info:" FontAttributes="Italic"/>
                            <Label Text="{Binding Brand}" TextColor="Red" FontAttributes="Bold"/>
                            <Label Text="{Binding Character}" TextColor="Blue" FontAttributes="Bold"/>
                            <Label Text="{Binding StringFormat=Issue}" TextColor="Green" FontAttributes="Italic"/>
                        </StackLayout>
                    </ViewCell.View>
                </ViewCell>
                
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>